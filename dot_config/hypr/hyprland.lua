-- Hyprland configuration — experimental second session alongside niri.
-- Mirrors the niri/Noctalia setup (../niri/config.kdl + cfg/*.kdl) as closely
-- as Hyprland's model allows. See docs/adr/0011 in the playground repo for
-- the why, every intentional gap/approximation, and the full removal steps.

require("config.environment")
require("config.autostart")
require("config.monitors")
require("config.inputs")
require("config.decorations")
require("config.animations")
require("config.binds")
require("config.windowrules")
require("config.workspaces")
